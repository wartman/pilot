package todo.ui;

// This is just an example of how vars might work.
// 
// Note that vars MUST be static, final, have an expression and
// be a string or an int.
class Color {
  public static final primary = '#4d4d4d';
  public static final secondary = '#f5f5f5';
}
